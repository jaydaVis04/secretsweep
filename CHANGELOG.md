# Changelog

## 0.1.1

- Improved cross-platform pre-commit hook fallback behavior
- Added `doctor` diagnostics command
- Added baseline file generation flag
- Enriched JSON output with metadata and rule catalog details
- Added release workflow and repository changelog

## 0.1.0

- Initial `secretsweep` CLI with offline scan, redact, init, install-hook, and doctor commands
- Regex-based and entropy-based detection
- Baseline load/write support
- Human-readable and JSON output modes
- Pre-commit hook installer
- Test suite, CI workflow, and release workflow
